# =============================================================================
# TESP Docker Configuration (moved)
# =============================================================================
# Configuration for original multi-Dockerfile build system

# -----------------------------------------------------------------------------
# Version Configuration
# -----------------------------------------------------------------------------
# Read from scripts/grid_version if available, or set manually
VER=24.04.1

# -----------------------------------------------------------------------------
# User Configuration
# -----------------------------------------------------------------------------
SIM_UID=1001
SIM_GID=9002
SIM_USER=worker
SIM_GRP=runner

BUILD_GRIDLABD=yes
BUILD_ENERGYPLUS=yes
BUILD_NS3=yes
BUILD_IPOPT=yes
BUILD_FNCS=yes
BUILD_HELICS=yes
BUILD_HELICS_PY=yes
BUILD_KLU=yes
BUILD_PSST=yes
BUILD_TESP=yes

# -----------------------------------------------------------------------------
# Build Configuration
# -----------------------------------------------------------------------------
UBUNTU_VERSION=24.04

# Build directory (where build scripts are located)
# Default is repository-relative path from `scripts/docker/` to `scripts/build`
BUILD_DIR=../build

# CPLEX Configuration (optional)
CPLEX_BIN=cplex_studio129.linux-x86-64.bin

# -----------------------------------------------------------------------------
# Runtime Environment Variables
# -----------------------------------------------------------------------------
PSST_SOLVER=cbc
PSST_WARNING=ignore

# -----------------------------------------------------------------------------
# Network Configuration (Optional)
# -----------------------------------------------------------------------------
# HTTPS_PROXY=http://proxy01.pnl.gov:3128
# HTTP_PROXY=http://proxy01.pnl.gov:3128

# -----------------------------------------------------------------------------
# Volume Mount Paths (paths relative to repository root when running from scripts/docker)
# -----------------------------------------------------------------------------
HOST_EXAMPLES_DIR=../examples
HOST_DATA_DIR=../data
HOST_OUTPUT_DIR=../outputs

# -----------------------------------------------------------------------------
# Build Options
# -----------------------------------------------------------------------------
COMPOSE_DOCKER_CLI_BUILD=1
DOCKER_BUILDKIT=1
