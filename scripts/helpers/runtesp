#!/bin/bash

IMAGE=cosim-build:tesp_22.04.1

docker run -it --rm \
       -e LOCAL_USER_ID=${SIM_UID} \
       --mount type=bind,source="$TESPDIR",destination="$DOCKER_HOME/tesp" \
       -w=$DOCKER_HOME \
       $IMAGE \
       bash
