Overview
========

This file documents the building up an ns-3 example using:

- FNSS to create the actual topology, chose to be a tree topology
- ns-3 Python bindings to incorporate the FNSS-built topology into an ns-3 model and run it.