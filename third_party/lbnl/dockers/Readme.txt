This folder contains 

- paramEstimation.py --> the main script for installing the docker images and running the estimation
- requirements.txt --> list of required python modules to be installed with pip install -r requirements.txt
- JModelica --> folder with JModelica docker files
- Estimation --> folder with parameter estimation docker files 
